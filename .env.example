# Frontend Environment Variables (for .env.frontend)
VITE_CLERK_PUBLISHABLE_KEY=your-clerk-publishable-key-here

MARIADB_ROOT_PASSWORD=root_password_here
MARIADB_PASSWORD=sql_password_here

# Admin API Key for sensitive operations (Required)
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
ADMIN_API_KEY=your-secure-admin-key-here

# Django Settings (recommended for production)
SECRET_KEY=your-django-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1

# JWT Secret Key (Required for API authentication)
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-jwt-secret-key-here

# Clerk Backend (needed by backend to fetch user info via Clerk API)
CLERK_SECRET_KEY=your-clerk-secret-key-here
CLERK_ISSUER=https://your-instance.clerk.accounts.dev

# cloudflare tunnel token
CLOUDFLARE_TOKEN=your-cloudflare-tunnel-token-here
